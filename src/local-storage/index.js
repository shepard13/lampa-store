import {
  setItemsAmount,
  setItemsPrice,
  setItemsCart,
} from './load-local-storage';
import {
  getItemsAmount,
  getItemsPrice,
  getItemsCart,
} from './save-local-storage';

export {
  setItemsAmount,
  setItemsPrice,
  setItemsCart,
  getItemsAmount,
  getItemsPrice,
  getItemsCart,
};
